# 07 · 分步搭建 · Web 接口与前端页面

> **执行状态**：⬜ 未开始 | 🟦 进行中 | ✅ 已完成 | ⏸️ 暂停 | ❌ 阻塞  
> **当前状态**：⬜  
> **适用范围**：MVP（对话页）+ 完整版（知识库管理页）  
> **负责人**：_（选填）_  
> **更新日期**：_（选填）_  
> **阻塞项**：无

### 本章验收
- [ ] 浏览器完成：上传 → 等待入库 → 提问 → 流式回答 + 引用
- [ ] 鉴权失败 / 空结果 / 服务错误有明确 UI
- [ ] SSE 在本地与经 Nginx（若已配）均可正常推送
- [ ] （完整版）任务进度与失败原因可展示

**阶段目标**：提供可交互对话页，完成端到端闭环。

契约：[03-数据模型与接口契约.md](./03-数据模型与接口契约.md)

---

## 步骤11：后端 API 接口开发

**状态**：⬜  

1. 实现 03 节契约；全局异常、参数校验、CORS  
2. 上传大小/类型限制；鉴权中间件  
3. 入库接口只入队，立即返回 `task_id`  
4. 健康检查、会话 CRUD  

**验收勾选**
- [ ] 非流式 `/api/v1/rag/chat`（域内）或终端 `POST /api/v1/apps/{id}/chat` 返回 answer + citations
- [ ] 流式事件：meta → token* → citations → done
- [ ] 非法 Token 返回 401/业务码
- [ ] 超大/非法类型文件在入队前被拒绝

---

## 步骤12：前端对话页面开发

**状态**：⬜  

1. Vue3 + Element Plus：左侧会话列表、右侧消息区、底部输入框  
2. 对接 SSE，打字机效果；断流提示与重试  
3. 回答下展示引用，可点击查看片段  
4. **完整版**：上传文件、提交 URL、任务进度、失败原因  
5. 空结果 / 鉴权失败 / 服务错误的明确 UI 状态  

**验收勾选**
- [ ] 多轮对话展示正常
- [ ] 引用可点击或展开
- [ ] （完整版）管理页可触发入库并看到任务状态

---

## 完成后

更新状态并进入 [08-联调评测与调优.md](./08-联调评测与调优.md)。  
部署注意 SSE 缓冲见 [09-部署上线.md](./09-部署上线.md)。
