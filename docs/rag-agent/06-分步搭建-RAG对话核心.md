# 06 · 分步搭建 · RAG 检索与对话核心

> **执行状态**：⬜ 未开始 | 🟦 进行中 | ✅ 已完成 | ⏸️ 暂停 | ❌ 阻塞  
> **当前状态**：⬜  
> **适用范围**：MVP（重排为可选增强）  
> **负责人**：_（选填）_  
> **更新日期**：_（选填）_  
> **阻塞项**：无

### 本章验收
- [ ] 检索强制 `kb_id` 过滤，跨库不串数据
- [ ] 能从 `app_id`（或 App 入口）解析 bindings → 允许的 `kb_id` 列表
- [ ] 流式对话：先 meta/token，后 citations，最后 done
- [ ] 无召回、模型失败有兜底（见附录 C）
- [ ] 至少 3 轮历史可带入且不超 Token 预算
- [ ] session 与 `kb_id` 不一致时拒绝请求

**阶段目标**：精准召回 + 可流式问答 + 引用来源。

依赖：[03-数据模型与接口契约.md](./03-数据模型与接口契约.md)、[appendix/C-Prompt与代码示例.md](./appendix/C-Prompt与代码示例.md)

---

## 步骤8：语义检索器

**状态**：⬜  

1. 查询向量化 → Top-K（带 `kb_id` 过滤）  
2. 元数据过滤：来源、时间、doc_id 列表  
3. 可选：向量 + 关键词多路召回，再经 reranker 重排  
4. 统一接口：`search(kb_id, query, top_k, filters) → List[ChunkHit]`  

**验收勾选**
- [ ] 指定 kb 能召回对应文档片段
- [ ] 错误 kb_id 或他库文档不会出现在结果中
- [ ] （可选）重排后 Top 结果相关性提升有记录

---

## 步骤9：Prompt 工程与上下文管理

**状态**：⬜  

1. 使用附录 C 中的 RAG Prompt 模板  
2. Token 预算：检索上下文 + 最近 N 轮 + 问题 ≤ `MAX_CONTEXT_TOKENS`；超长优先截断低分片段  
3. 历史：默认最近 `HISTORY_MAX_TURNS`；过长可摘要（演进）  
4. 引用：返回 chunk 来源供前端展示  

**验收勾选**
- [ ] 无足够上下文时不编造（拒答/兜底）
- [ ] 超长上下文不会打爆模型上下文窗口

---

## 步骤10：对话全流程编排（Manager）

**状态**：⬜  

1. **解析 `app_id`（若来自 App 入口或请求携带）**：加载 App → 读 `bindings_json` → 得到允许的 `kb_ids` / `prompt_id` / retrieve 参数；无 `app_id` 的域内调试请求须显式 `kb_id`，且不得绕过后续隔离校验  
2. 确定本次使用的 `kb_id`（须 ∈ bindings 或调试显式指定），校验 session 与 `kb_id` 一致  
3. 检索 → 组装 Prompt → 流式 LLM → 累积完整答案 → 保存消息与 citations（建议落库可追溯 `app_id`）  
4. 无召回 / 模型失败走兜底  

**验收勾选**
- [ ] 经 `apps/{id}/chat` 进入时，kb 来自 bindings，而非前端长期写死裸 `kb_id`
- [ ] SSE 事件顺序符合 03 契约
- [ ] 落库后会话详情能还原问答与引用（含 app 上下文若有）
- [ ] 示例逻辑中答案已完整累积（对照附录 C）

---

## 关键场景速查

详见附录 C「关键场景与兜底」；实现时至少覆盖：无结果、模型 5xx、kb 停用、session 串库。

---

## 完成后

更新状态并进入 [07-分步搭建-Web前后端.md](./07-分步搭建-Web前后端.md)。
