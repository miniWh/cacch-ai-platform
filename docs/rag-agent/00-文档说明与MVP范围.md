# 00 · 文档说明与 MVP 范围

> **执行状态**：⬜ 未开始 | 🟦 进行中 | ✅ 已完成 | ⏸️ 暂停 | ❌ 阻塞  
> **当前状态**：⬜  
> **适用范围**：共用  
> **负责人**：_（选填）_  
> **更新日期**：_（选填）_  
> **阻塞项**：无

### 本章验收
- [ ] 团队已确认 MVP / 完整版边界
- [ ] 已确认 MVP 五条验收标准
- [ ] 已确认仓库与命名约定
- [ ] 已按推荐顺序开始阅读 01

---

## 1. 读者与目标

面向需要**从零落地一套检索增强（RAG）对话智能体**的后端/全栈工程师。

交付闭环：**多源知识入库 → 语义检索 → 大模型问答 → Web 对话（含引用来源）**。

## 2. 与「完整 Agent」的边界

本文档体系覆盖 **RAG 对话版**：以知识库问答为主。

**不包含**：工具调用、多步任务规划、复杂 Agent 编排。相关能力见 [11-后续演进与FAQ.md](./11-后续演进与FAQ.md)。

## 3. MVP 与完整版

| 范围 | 包含能力 | 建议工期（参考） |
| :--- | :--- | :--- |
| **MVP** | 本地文件入库、单知识库检索问答、多轮对话、SSE 流式、引用来源、基础会话历史 | 1.5～3 周 |
| **完整版** | + 网站爬取、业务库同步、知识库管理页、增量更新、异步入库任务、评测与调优 | 再 +2～4 周 |

### MVP 验收标准（必须全部通过）

1. 上传 PDF/Word/TXT/Markdown，入库成功且可在管理接口查询文档状态  
2. 基于该知识库提问，能召回相关片段并生成回答  
3. 回答下方能展示引用来源（文件名/页码或 chunk 摘要）  
4. 支持 SSE 流式输出与至少 3 轮上下文记忆  
5. 空检索、模型超时有明确兜底文案，不白屏/不裸报错  

## 4. 项目目标与能力

### 核心目标

**多源数据采集 → 清洗切片 → 向量存储 → 语义检索 → 大模型问答 → 前端交互**

用户通过 Web 对话页，基于指定内部知识库提问，并溯源到具体文档片段。

### 覆盖能力

- **多源数据接入**：本地文档（MVP）、网站爬取、业务库同步（完整版）
- **向量知识库**：切片、向量化、异步入库、增量更新、版本与删除一致性
- **智能问答**：语义召回（可选多路+重排）、上下文拼接、流式生成、对话记忆、引用
- **Web**：对话、流式展示、引用、知识库管理（完整版）

### 非目标（本版不做）

- Function Calling / 多 Agent 协作  
- 多租户计费、复杂权限（仅预留 `user_id` / `kb_id`）  
- 图片 OCR、知识图谱（演进项）  

## 5. 仓库与命名

- 平台仓库：`cacch-ai-platform`  
- 智能体工程建议目录：`cacch-ai-agent`（子项目或独立服务）  
- Python 包前缀：`cacch_ai_xxx`  
- 服务名：`cacch-ai-领域-service`（如 `cacch-ai-rag-service`）  

## 6. 推荐阅读 / 执行顺序

见 [README.md](./README.md#推荐执行顺序)。本章确认后进入 [01-前期准备工作.md](./01-前期准备工作.md)。
