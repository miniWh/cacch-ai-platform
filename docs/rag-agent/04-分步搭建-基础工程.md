# 04 · 分步搭建 · 基础工程与环境初始化

> **执行状态**：⬜ 未开始 | 🟦 进行中 | ✅ 已完成 | ⏸️ 暂停 | ❌ 阻塞  
> **当前状态**：⬜  
> **适用范围**：MVP  
> **负责人**：_（选填）_  
> **更新日期**：_（选填）_  
> **阻塞项**：无

### 本章验收
- [ ] 服务可启动，`GET /api/v1/health` 返回 ok
- [ ] 关系库建表完成，可读写
- [ ] 向量库可插入测试向量，并按 `kb_id` 召回
- [ ] LLM 流式与 Embedding 单测通过，维度 = 配置值

**阶段目标**：搭建可运行骨架，连通全部中间件，打通模型能力。

依赖：[01-前期准备工作.md](./01-前期准备工作.md)、[02-整体架构与结构.md](./02-整体架构与结构.md)、[03-数据模型与接口契约.md](./03-数据模型与接口契约.md)、[appendix/B-配置与Compose模板.md](./appendix/B-配置与Compose模板.md)

---

## 步骤1：初始化 Python 工程

**状态**：⬜  

1. 使用 Poetry 创建项目，锁定 Python 版本与基础依赖  
2. 按 [02](./02-整体架构与结构.md) 目录结构创建包与空文件，落实单向依赖  
3. 复制 `.env.example` → `.env`，封装多环境配置加载（dev/test/prod）  
4. 增加健康检查接口 `GET /api/v1/health`  

**验收勾选**
- [ ] `poetry install` / 依赖安装成功
- [ ] 配置能读到 `EMBEDDING_DIM`、数据库 DSN
- [ ] 健康检查可用

---

## 步骤2：数据库与中间件连通

**状态**：⬜  

1. 关系库连接类 + ORM 基类；执行建表迁移（见 03 表结构）  
2. 向量库客户端：建集合/索引、upsert、按 `kb_id` 过滤检索、按 `doc_id` 删除  
3. Redis 连接：任务队列与可选缓存  
4. Compose 一键拉起依赖（附录 B）  

**验收勾选**
- [ ] 核心表已创建（kb / document / chunk / session / message / ingest_task 等）
- [ ] 插入一条测试向量并可按 `kb_id` 召回
- [ ] Redis ping 成功

---

## 步骤3：大模型能力接入

**状态**：⬜  

1. 抽象接口：`chat` / `chat_stream` / `embed_batch`  
2. 厂商 SDK 适配：超时、重试、错误映射  
3. 单测：单轮对话、流式输出、向量维度符合 `EMBEDDING_DIM`  

**验收勾选**
- [ ] 流式可打印 token
- [ ] Embedding 返回长度 = 配置维度
- [ ] 启动时校验维度与向量集合一致（不匹配则拒绝写入）

---

## 完成后

更新本文文首状态为 ✅，并同步 [README.md](./README.md) 进度表。  
进入 [05-分步搭建-数据入库.md](./05-分步搭建-数据入库.md)。
