---
description: Modern Python coding standards for this platform
globs: "**/*.py"
alwaysApply: false
---

# Python Core

- Target Python 3.12+ syntax.
- Annotate all public function parameters and return types.
- Prefer `X | None` over `Optional[X]`; use built-in generics (`list[str]`, `dict[str, int]`).
- **禁止使用 `Any` / `typing.Any`**（含变量注解、参数、返回值、`cast`、`dict[str, Any]` 等）。用具体类型、`TypedDict`、Protocol、泛型或 `object` 替代。
- Use f-strings for formatting; prefer `pathlib.Path` over `os.path`.
- Use context managers for files, DB sessions, HTTP clients, and locks.
- Do not swallow exceptions with bare `except:` or empty `except Exception:`.

```python
# ❌ BAD — 禁止 Any
from typing import Any

def parse(payload: dict[str, Any]) -> Any:
    return payload.get("data")

# ✅ GOOD
from typing import TypedDict

class Payload(TypedDict):
    data: str

def parse(payload: Payload) -> str:
    return payload["data"]
```

```python
# ❌ BAD
def load(path):
    try:
        return open(path).read()
    except:
        return None

# ✅ GOOD
def load(path: Path) -> str:
    with path.open(encoding="utf-8") as f:
        return f.read()
```
