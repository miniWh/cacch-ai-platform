---
description: 每次输出代码后须列出修改文件的阅读顺序并说明
alwaysApply: true
---

# 代码交付：阅读顺序

凡本轮对话中**新增或修改了代码文件**（含脚本、前后端、配置），在最终回复中必须：

1. 列出**建议阅读顺序**（编号列表，路径用仓库相对路径）。
2. 每个文件用**一两句中文**说明：改了什么 / 为何先读它。
3. 顺序原则：需求/契约或 SQL（若有）→ 模型/核心逻辑 → 服务 → API/入口 → 前端 → 测试。
4. 若仅改文档或规则、无业务代码，可省略阅读顺序，或只列文档阅读顺序。

示例：

```markdown
### 修改文件阅读顺序
1. `app/rag/loader/fetch.py` — 抓取与正文抽取核心
2. `app/service/site_fetch_service.py` — 按站点清单编排
3. `scripts/fetch_sites.py` — 控制台入口
4. `tests/test_site_fetch.py` — 用例
```
