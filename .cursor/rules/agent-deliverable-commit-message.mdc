---
description: 修改项目后须输出中文 Commit 文案草稿，禁止擅自 git commit
alwaysApply: true
---

# 代码交付：中文 Commit 文案

凡本轮对话中**新增或修改了项目文件**（代码、脚本、配置、规则、文档等），在最终回复中必须：

1. 输出本次修改的 **Commit 文案（中文）**，供用户自行提交。
2. **不要**执行 `git commit` / `git add`（除非用户明确要求提交）。
3. 文案格式建议：
   - 第一行：简短标题（可用约定式前缀，如 `feat` / `fix` / `docs` / `chore`）
   - 可选第二段：1～3 句说明动机与影响（为何改，而非逐文件罗列）
4. 标题聚焦「为什么」，准确对应改动性质（新增 / 修复 / 文档 / 规则等）。

示例：

```markdown
### Commit 文案（未提交）

\`\`\`
feat(rag): 按站点清单抓取入口页并打印控制台

新增 fetch 加载器与 CLI，抓取结果暂不落库，便于联调站点正文抽取。
\`\`\`
```
